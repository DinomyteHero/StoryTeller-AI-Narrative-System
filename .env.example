# ── Backend mode ──────────────────────────────────────────────────────────────
# cloud → DeepSeek V4 Flash + Pro via OpenRouter (recommended)
# local → Ollama for everything (offline dev / fallback)
NARRATIVE_BACKEND=cloud

# ── Cloud provider & API key (NARRATIVE_BACKEND=cloud) ────────────────────────
# Default path: OpenRouter with DeepSeek V4 models.
CLOUD_PROVIDER=openrouter
OPENROUTER_API_KEY=sk-or-...

# Two-tier model routing (preferred):
#   FAST    — structured JSON, decisions, annotations, reconciliation.
#             High-volume, latency-sensitive. Bounded by JSON schema.
#   QUALITY — narration, milestone reflections, time-skip prose, studio gen.
#             Quality-critical, latency-tolerant.
FAST_MODEL=deepseek/deepseek-v4-flash
QUALITY_MODEL=deepseek/deepseek-v4-pro

# Per-call-site overrides (optional). Empty = inherit from tier default.
# Set these to A/B individual calls without touching tier config.
# NARRATION_MODEL=
# DECISION_MODEL=
# ANNOTATION_MODEL=
# RECONCILIATION_MODEL=
# DIAGNOSTIC_MODEL=
# MILESTONE_MODEL=
# STUDIO_MODEL=

# OpenRouter app attribution (shows up in your dashboard).
OPENROUTER_APP_NAME=Storyteller-V3
OPENROUTER_APP_URL=https://github.com/storyteller-v3

# Legacy single-cloud-model knob — if set, overrides QUALITY_MODEL.
# Kept for back-compat with older deployments. Leave commented unless needed.
# CLOUD_MODEL=gpt-5.2

# Direct OpenAI (only if CLOUD_PROVIDER=openai).
# OPENAI_API_KEY=sk-...

# ── Token / reasoning budgets ─────────────────────────────────────────────────
MAX_COMPLETION_TOKENS=16000

# Only honored by reasoning models (gpt-o*, gpt-5*). Ignored by DeepSeek.
REASONING_EFFORT=low

# ── Local fallback (Ollama) ───────────────────────────────────────────────────
# Used when NARRATIVE_BACKEND=local OR when cloud narration fails and the
# emergency local fallback kicks in.
OLLAMA_URL=http://localhost:11434
LOCAL_FAST_MODEL=qwen3.5:9b
LOCAL_QUALITY_MODEL=ministral-3:8b

# Legacy local-model env vars (still honored; LOCAL_MODEL == LOCAL_FAST_MODEL).
# LOCAL_MODEL=qwen3.5:9b
# LOCAL_NARRATION_MODEL=ministral-3:8b

# ── Database ──────────────────────────────────────────────────────────────────
DB_PATH=./data/storyteller.db

# ── Server ────────────────────────────────────────────────────────────────────
PORT=8000
DEV_MODE=true
STREAMING_ENABLED=true

# ── Prose voice ───────────────────────────────────────────────────────────────
# clean    — direct, grounded (default)
# literary — Stover/Luceno blend
PROSE_VOICE=clean
